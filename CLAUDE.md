# CLAUDE.md

# Rules

- On every `.mq5` file modification, bump `#property version` following semver `MAJOR.MINOR.PATCH`: MAJOR for breaking changes, MINOR for new functionality, PATCH for fixes or non-functional changes. This does not apply to `.mqh` files. - or previous to commit/push changes, we should update the version.
